services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: cwfgw
      POSTGRES_USER: cwfgw
      POSTGRES_PASSWORD: cwfgw
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
